#!/bin/sh
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd -P)
case "${1-}" in
  verify-release|--help|-h|help) exec node "$root/dist/cli.js" "$@" ;;
  *) exec node "$root/dist/server.js" ;;
esac
